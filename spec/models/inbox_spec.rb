require File.expand_path(File.dirname(__FILE__) + '/../spec_helper')

describe Inbox do
  before(:each) do
    @valid_attributes = {
    }
  end

  it "should create a new instance given valid attributes" do
    pending "UserMonitor needs to be addressed"
    Inbox.create!(@valid_attributes)
  end
end
